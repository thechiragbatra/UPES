// Online C compiler to run C program online
#include <stdio.h>

float main() {
    // Write C code hereConvert Temperature Celsius into Fahrenheit
    int celcius,fahrenheit;
    printf("enter temperature in celcius");
    scanf("%f",&celcius);
    printf("temperature in fahreneit is:%f",celcius*1.8+32);

    return 0;
}